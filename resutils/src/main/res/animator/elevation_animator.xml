<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:state_selected="true">
        <objectAnimator
            android:duration="200"
            android:propertyName="elevation"
            android:valueTo="6dp"
            android:valueType="floatType" />
    </item>

    <item>
        <objectAnimator
            android:duration="200"
            android:propertyName="elevation"
            android:valueTo="0dp"
            android:valueType="floatType" />
    </item>

    <!--add attribute to elevated element -> android:stateListAnimator="@animator/elevation_animator"-->
    <!--select element in scrolllistener to trigger elevation -->
    <!--scroll_view.setOnScrollChangeListener { _, _, _, _, _ ->--><!--header.isSelected = scroll_view.canScrollVertically(-1)--><!--}-->
</selector>